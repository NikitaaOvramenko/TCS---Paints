# Data Model: UI Styling Refresh

> No new entities introduced. Styling changes are visual and do not affect data.

## Notes

- Centralize design tokens (colors, spacing, typography) in `theme.css`.
- Components consume tokens via CSS variables or CSS Modules.

